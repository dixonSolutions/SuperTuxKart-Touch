<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          android:installLocation="auto">

    <application android:label="@string/app_name"
                 android:icon="@drawable/icon"
                 android:allowBackup="true"
                 android:appCategory="game"
                 android:banner="@drawable/banner"
                 android:hasCode="true"
                 android:isGame="true"
                 android:theme="@style/Theme.STKSplashScreen"
                 android:hardwareAccelerated="true"
                 android:resizeableActivity="true">

        <activity android:name=".SuperTuxKartActivity"
                  android:label="@string/app_name"
                  android:launchMode="singleTask"
                  android:configChanges="fontScale|keyboard|keyboardHidden|locale|mcc|mnc|navigation|orientation|screenLayout|screenSize|uiMode|layoutDirection|grammaticalGender|fontWeightAdjustment|smallestScreenSize"
                  android:screenOrientation="sensorLandscape"
                  android:preferMinimalPostProcessing="true"
                  android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <!-- Let Android know that we can handle some USB devices and should receive this event -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
        </activity>
    </application>

    <uses-feature android:glEsVersion="0x00020000" />
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.software.input_methods" android:required="false" />

    <!-- Touchscreen support -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- Game controller support -->
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.gamepad"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <!-- External mouse input events -->
    <uses-feature
        android:name="android.hardware.type.pc"
        android:required="false" />

    <!-- No storage, Bluetooth, location or other runtime ("dangerous")
         permission, on purpose: nothing here makes Android show a prompt.
         Game data, config and add-ons live in the app's own directories
         (Context.getExternalFilesDir / getFilesDir), which need no
         permission on any supported Android version; see
         src/io/assets_android.cpp. Bluetooth gamepads and keyboards reach
         the game through the input system and need no permission either;
         only SDL's direct Steam Controller BLE driver would, and it simply
         stays off. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Only to ask whether the active network is metered, so the automatic
         update path does not pull a whole APK over mobile data unasked. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Sideloaded builds have no store to update them, so the app offers its
         own release feed on launch. Android still confirms every install. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Android 12+: once this app has installed one of its own updates it is
         the installer of record, and later updates can skip the confirm
         dialog. Without the permission setRequireUserAction is ignored. -->
    <uses-permission android:name="android.permission.UPDATE_PACKAGES_WITHOUT_USER_ACTION" />

    <!-- Gamepad rumble: SDL drives the controller's motor through
         InputDevice.getVibrator(), which throws without this. A normal
         install-time permission; it never prompts. -->
    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>
